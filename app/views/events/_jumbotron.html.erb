<!-- Main jumbotron for a primary marketing message or call to action -->
      <div class="jumbotron">
        <div class="container">
          <h1 class="display-3"> <%= @event.name %> <%= t(:from) %> <%= l @event.start_date, format: :long %> <%= t(:to) %>  <%= l @event.end_date, format: :long %></h1>
          <p>We need to add some infos here...</p>
          <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more &raquo;</a></p>
        </div>
      </div>
